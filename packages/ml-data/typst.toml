[package]
name = "ml-data"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["Nipun Batra"]
license = "MIT"
description = "A tiny data-frame for Typst — load CSV/JSON/arrays into named columns, select/filter/mutate/group, and hand columns straight to ml-plot. So a figure plots the data, not a guess."
keywords = ["data-frame", "csv", "table", "plot", "teaching"]
