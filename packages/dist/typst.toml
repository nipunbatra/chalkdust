[package]
name = "dist"
version = "0.1.0"
entrypoint = "lib.typ"
authors = ["Nipun Batra"]
license = "MIT"
description = "Standard probability distributions for Typst — exact pdf / log-pdf / nll for Normal, Laplace, Student-t, Uniform, Bernoulli, Categorical … so a loss curve is the TRUE negative log-likelihood, not a hand-tuned coefficient."
keywords = ["probability", "distribution", "likelihood", "deep-learning", "teaching"]
